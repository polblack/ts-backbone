/**
 * 
 * Componet Dashboard
 * 
 */

import * as $ from "jquery";
import * as _ from "underscore";
import * as Backbone from "backbone";
import { component, ComponentBase } from "../../backbonelib/core";
@component({
    templateUrl:`this is the test content`,
    selector:'test'
})
export class TestComponent{ 
    Init(){

    }
  
}

